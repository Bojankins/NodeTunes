extends ../shared/template

block content
  h1 New Artist
  form(method='POST', action='/artists', enctype='multipart/form-data')
    input(type='text', name='name', placeholder='New Artist Name')
    input.file(type='file', name='photo', multiple=false)
    button Add Artist